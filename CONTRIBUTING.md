# 实验变更约定

修改方法、数据处理、模型参数或提示词时，保留原配置并新建运行目录。不要覆写已经锁定的测试输出；不要根据测试目标筛选最好种子。新增字段必须通过输入白名单审计。所有 bug 修复应配套最小回归测试，运行 `python -m pytest -q` 和对应 demo。

不要提交个人原始视频、访问令牌、第三方受限权重或不允许重新分发的数据。`LICENSE` 只覆盖本库原创代码，不替代数据、模型或外部仓库的授权。
