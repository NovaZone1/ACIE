# 数据与评价协议

## 统一数据接口

每个原始窗口保存为不含 Python 对象的 NPZ。清单每行必需字段为 `sample_id`、`dataset`、`recording`、`track_id`、`pool`、`label`、`window`、`panoramic`。`pool` 只能为 `train/test/all`，`label` 为 0/1。可选 `group_id` 指定比单个 recording 更保守的划分与 bootstrap 单元；本次源域对照使用日期。窗口路径限制在清单所在根目录，越界路径被拒绝。

处理后的 `bundle.npz` 包含 a、q、y；同名 JSON 包含元数据、哈希、特征名称和来源。整库不得混用不同 T；不同提前量或窗口长度应分别导出。特征文件没有 `oracle`、交互等级、未来事件位置或答案参数列。

## 划分口径

| 入口 | 使用方式 | 必须使用的结果名称 |
|---|---|---|
| `acie split`，source≠target | 源域 train/all 池留出录制级开发；目标 test/all 池评分 | 严格源域选模跨域协议 |
| `acie split`，source=target | 全域样本按录制重新划分训练／开发／测试 | 本地域内分组协议 |
| `split_from_pools.py` | 保持显式 test 池，开发集仅来自 train 池 | 固定测试池、源域分组开发协议 |
| 原作者 `training.py` | 运行其完整配置与采样行为 | 对应上游 commit 的原生结果 |

所有本库划分检查录制和轨迹交叉。官方轨迹划分若允许同一录制跨边界，本库严格检查会拒绝；不得为保留“原生”字样跳过检查。可以独立运行原作者配置并明确其口径，但不能宣称两种协议相同。

官方 loader 可逐次随机取窗口。本库导出将具体选中帧冻结到清单，因此相同样本清单上的方法比较可复查；不宣称复制了原作者逐轮随机采样轨迹。`export_provenance.json` 保存参数、上游 commit、帧率及输入来源。

## 指标

AP 使用 scikit-learn 的 average_precision_score，不以梯形 PR-AUC 替代。AUROC 在测试标签只有一类时记录为空。阈值从源域开发 F1 选择并冻结；真实目标域不得重新调阈值。目标域配对只生成事先冻结规则下的诊断统计，不反馈训练。

置信区间按清单声明的 `group_id` 有放回重采样；本轮跨域评估的 `group_id` 是日期，缺失时才退回录制单元。该做法保留组内相关样本；比较两个方法时要求完全相同 ID 与标签。正例比例、样本数和匹配覆盖必须与 AP 一起报告。组数太少时区间不稳定，不能将其解释为总体保障。

2026-09-14 的双向跨域评估在首次目标评分前保存 `FROZEN_PROTOCOL.json`，锁定合并数据、四份配置、五个种子和两份 split 的 SHA-256。目标池不参与早停或配置选择。冻结 revision 缺少配置列出的三个录制，相关结果统一标为“可用官方配置子集”；具体名称和方向见 `reports/frozen_cross_domain_evaluation_2026-09-14.md`。

## 版本与授权

原始视频的审批要求与处理后数据不同。本库只实现处理后数据途径，不复制原始视频、第三方预训练权重或第三方源代码。先审查原作者条款再下载。下载 lock、官方 commit、模型配置、样本清单、split JSON 和环境版本应随研究结果归档。
