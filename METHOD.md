# 方法实现

## 数据流

一个合法前缀包含归一化图像坐标下的 `keypoints[T,17,3]`、`bbox[T,4]`、`mask_area[T]`、`times[T]`。关键点最后一维为 x、y、置信度；边界框为 xmin、ymin、xmax、ymax；面积为图像面积的比例；时间单位为秒。允许全景边界框跨越左右接缝。缺失点以置信度及有效位处理，预测器只接收显式构造的数组。

几何通道由中心方位正弦／余弦、纵向中心、框宽高和面积的对数、可见掩码比例的对数、角速度构成，各计算末值／均值／标准差／斜率。局部行为由身体相对关节位置、置信度、关节速度、位置有效位和速度有效位构成。尺度是距离代理，不转换成无标定依据的米制距离。头部关节不能等同于眼动。

标准化器只在源域训练行拟合。持续特征的缺失位在变换后恢复为零，有效位保留。录制与轨迹 ID 用于划分和配对，不进入模型。

## 主方法

对第 i 个观察前缀，几何特征记为 a_i，局部行为记为 q_i，输出为

$$p_i=\sigma(g_\phi(a_i)+r_\theta(q_i)).$$

其中 g 为几何分支，r 为行为增量分支，φ、θ 为参数，σ 为 sigmoid。先使用源域训练数据拟合 g，并按源域开发 AP 选择检查点，再冻结 g 训练 r。几何 MLP 和局部时序卷积都是常规组件。

在源域标准化几何空间中寻找异标签、异轨迹邻居。距离使用欧氏距离除以维数平方根；逐维差值、源域半径、负轨迹复用上限共同形成约束。半径可固定，也可由源域训练集最近可匹配邻居距离的分位数拟合。无匹配样本继续参加分类训练。

$$\mathcal L=\mathcal L_{\mathrm{BCE}}+\lambda\frac{\sum_{(i,j)\in\mathcal M}w_{ij}\log(1+\exp[-(r_i-r_j)])}{\sum_{(i,j)\in\mathcal M}w_{ij}}.$$

M 为配对集合，i 为正例、j 为负例，w 为几何距离权重，λ 为开发集确定的排序权重。实现中分类批次从原始分布打乱取样，配对批次独立抽样。空配对项返回零；不因低支持覆盖而悄悄修改主任务样本。

冻结几何模型避免两分支任意交换解释份额，但不提供因果识别。折外几何预测是研究方案中的可选扩展，当前主实现没有把它包装成已完成组件。

## 归因对照

`dual_no_pair` 保持相同双路结构并删除配对项；`fusion_pair` 对同特征融合 MLP 施加配对监督；`random_pair` 从主方法实际配对出发交换负例，保持正例数、权重和负轨迹复用数，隔离配对身份的作用。匹配审计给出随机对照实际替换比例。

`random_pair` 和 `hard_negative` 使用与完整模型相同的冻结几何加行为残差结构；前者仅交换配对负例身份，后者仅从源域训练预测中选择高分负例并排除同轨迹。`resampled` 只改变分类抽样，`weighted` 只做类别损失加权。树模型接收同样可用信息的末值、均值和标准差，并只按源域开发 AP 选择三组结构超参数。

## 当前论文主张与证据

当前稳健主张是：接近几何提供基础排序，冻结几何后学习且与样本正确对应的行为残差，在约1.07秒的官方观察截断处可在两个跨域方向提供额外排序证据。证据来自同容量 `geometry`／`dual_no_pair` 对照、行为对应关系置换、两个迁移方向和E4共同轨迹分析。

条件配对损失没有通过双向判据，因此保留为受检验但未获支持的研究接口，不列为论文核心贡献。1.60—3.20秒的E4差值点估计虽均为正，但聚类区间跨0；源域阈值在目标域的召回率也很低。论文不据此主张更长提前量的稳定收益或可部署的报警性能。E3 局部配对诊断的中心模型在两个方向仅呈小幅且高方差的最终排序改善；同结构随机配对相对正确配对也没有双向一致差异，因此这些补充结果继续支持删除配对贡献。HistGB 在反向达到 0.4712 AP、前向为 0.1318，说明简单非线性基线同样具有明显方向依赖；论文不主张 ACIE 全面优于简单模型。图模型本身、分类损失及通用排序函数不单列创新。
